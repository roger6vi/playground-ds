# Adaptadores de componentes

Los adaptadores permiten consumir los Web Components del Design System desde diferentes frameworks.
Organiza los adaptadores por framework (por ejemplo, `react`, `vue`, `angular`).

Actualmente solo se crea la carpeta `react/` como placeholder.
